<div class="row caption">
	<div class="col-md-6">
		<p align="center"><%= @listing.name %></p>
		<p align="center"><%= number_to_currency(@listing.price) %></p>
		<p align="center"><%= @listing.description %></p>
    <div class="thumbnail">
	<%= image_tag @listing.image.url %>
</div>

	</div>
	<div class="col-md-6">
		
		<br>
		
	</div>
</div>


<%= link_to 'Edit', edit_listing_path(@listing) %> |
<%= link_to 'Back', listings_path %>
